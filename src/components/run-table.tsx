"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import type { NormalizedRun } from "../lib/sources/types";

type SortKey = "cost" | "score" | "date" | "duration" | "model" | "task";
type Filters = { search: string; source: string; model: string; task: string; harness: string; status: string; sort: SortKey; direction?: "asc" | "desc" };
const dash = (value: string | number | null) => value ?? "—";

export function filterRuns(runs: NormalizedRun[], filters: Filters) {
  const search = filters.search.toLowerCase().trim(); const direction = filters.direction ?? "desc";
  const key = filters.sort === "cost" ? "totalCostUsd" : filters.sort === "score" ? "score" : filters.sort === "duration" ? "durationMs" : filters.sort === "date" ? "createdAt" : filters.sort;
  return runs.filter((run) => (!filters.source || run.sourceId === filters.source) && (!filters.model || run.model === filters.model) && (!filters.task || run.task === filters.task) && (!filters.harness || run.harness === filters.harness) && (!filters.status || run.status === filters.status) && (!search || [run.sourceId, run.model, run.provider, run.harness, run.task].some((value) => value?.toLowerCase().includes(search)))).sort((left, right) => { const a = left[key] ?? (direction === "asc" ? Infinity : -Infinity); const b = right[key] ?? (direction === "asc" ? Infinity : -Infinity); const result = typeof a === "string" && typeof b === "string" ? a.localeCompare(b) : Number(a) - Number(b); return direction === "asc" ? result : -result; });
}

export function RunTable({ runs }: { runs: NormalizedRun[] }) {
  const [filters, setFilters] = useState<Filters>({ search: "", source: "", model: "", task: "", harness: "", status: "", sort: "cost", direction: "desc" });
  const visible = useMemo(() => filterRuns(runs, filters), [runs, filters]);
  const options = (field: "sourceId" | "model" | "task" | "harness") => [...new Set(runs.flatMap((run) => run[field] ? [run[field] as string] : []))].sort();
  const select = (label: string, field: keyof Pick<Filters, "source" | "model" | "task" | "harness" | "status">, values: string[]) => <label className="grid gap-1 text-xs font-medium text-slate-400">{label}<select className="rounded border border-slate-700 bg-slate-950 px-3 py-2 text-sm text-slate-100" value={filters[field]} onChange={(event) => setFilters({ ...filters, [field]: event.target.value })}><option value="">All</option>{values.map((value) => <option key={value} value={value}>{value}</option>)}</select></label>;
  return <div className="grid items-start gap-6 lg:grid-cols-[260px_minmax(0,1fr)]"><aside className="sticky top-5 rounded-xl border border-slate-800 bg-slate-900/60 p-4"><div className="mb-4"><h2 className="font-semibold text-white">Filters</h2><p className="mt-1 text-xs text-slate-400">{visible.length} of {runs.length} runs</p></div><div className="grid gap-4"><label className="grid gap-1 text-xs font-medium text-slate-400">Search<input className="rounded border border-slate-700 bg-slate-950 px-3 py-2 text-sm text-slate-100" value={filters.search} onChange={(event) => setFilters({ ...filters, search: event.target.value })} placeholder="Model, task…"/></label>{select("Task", "task", options("task"))}{select("Source", "source", options("sourceId"))}{select("Model", "model", options("model"))}{select("Harness", "harness", options("harness"))}{select("Status", "status", ["success", "failed", "partial", "timeout", "unknown"])}<label className="grid gap-1 text-xs font-medium text-slate-400">Sort by<select className="rounded border border-slate-700 bg-slate-950 px-3 py-2 text-sm text-slate-100" value={filters.sort} onChange={(event) => setFilters({ ...filters, sort: event.target.value as SortKey })}><option value="cost">Cost</option><option value="score">Score</option><option value="date">Date</option><option value="duration">Duration</option><option value="model">Model</option><option value="task">Task</option></select></label><div className="grid grid-cols-2 gap-2"><button className={`rounded border px-2 py-2 text-xs ${filters.direction === "desc" ? "border-sky-400 bg-sky-400/15 text-sky-100" : "border-slate-700 text-slate-300"}`} onClick={() => setFilters({ ...filters, direction: "desc" })}>Descending</button><button className={`rounded border px-2 py-2 text-xs ${filters.direction === "asc" ? "border-sky-400 bg-sky-400/15 text-sky-100" : "border-slate-700 text-slate-300"}`} onClick={() => setFilters({ ...filters, direction: "asc" })}>Ascending</button></div><button className="text-left text-xs text-sky-300" onClick={() => setFilters({ search: "", source: "", model: "", task: "", harness: "", status: "", sort: "cost", direction: "desc" })}>Reset filters</button></div></aside><div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full text-left text-sm"><thead className="bg-slate-900 text-slate-400"><tr>{["Result", "Model", "Source", "Provider", "Harness", "Task", "Status", "Score", "Cost", "Duration", "Tokens", "Created"].map((label) => <th className="p-3" key={label}>{label}</th>)}</tr></thead><tbody>{visible.map((run) => <tr className="border-t border-slate-800 hover:bg-slate-900/70" key={run.id}><td className="p-3"><Link className="font-medium text-sky-300" href={`/runs/${run.id}`}>View</Link></td><td className="p-3">{dash(run.model)}</td><td className="p-3">{run.sourceId}</td><td className="p-3">{dash(run.provider)}</td><td className="p-3">{dash(run.harness)}</td><td className="p-3">{dash(run.task)}</td><td className="p-3">{run.status}</td><td className="p-3">{dash(run.score)}</td><td className="p-3">{run.totalCostUsd === null ? "—" : `$${run.totalCostUsd.toFixed(3)}`}</td><td className="p-3">{run.durationMs === null ? "—" : `${run.durationMs}ms`}</td><td className="p-3">{dash(run.totalTokens)}</td><td className="p-3">{run.createdAt ? new Date(run.createdAt).toLocaleDateString() : "—"}</td></tr>)}</tbody></table></div></div>;
}
