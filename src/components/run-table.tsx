"use client";
import Link from "next/link";
import { useMemo, useState } from "react";
import type { NormalizedRun } from "../lib/sources/types";
const dash = (value: string | number | null) => value ?? "—";
export function RunTable({ runs }: { runs: NormalizedRun[] }) { const [search, setSearch] = useState(""); const [status, setStatus] = useState(""); const visible = useMemo(() => runs.filter((run) => (!status || run.status === status) && (!search || [run.model, run.task, run.sourceId, run.harness].some((value) => value?.toLowerCase().includes(search.toLowerCase())))), [runs, search, status]); return <><div className="mb-4 flex gap-3"><input aria-label="Search runs" className="rounded border border-slate-700 bg-slate-950 px-3 py-2" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search model, task…"/><select aria-label="Filter status" className="rounded border border-slate-700 bg-slate-950 px-3 py-2" value={status} onChange={(event) => setStatus(event.target.value)}><option value="">All statuses</option>{["success", "failed", "partial", "timeout", "unknown"].map((value) => <option key={value}>{value}</option>)}</select></div><div className="overflow-x-auto rounded-lg border border-slate-800"><table className="w-full text-left text-sm"><thead className="bg-slate-900 text-slate-400"><tr>{["Model", "Source", "Task", "Status", "Score", "Cost", "Duration"].map((label) => <th className="p-3" key={label}>{label}</th>)}</tr></thead><tbody>{visible.map((run) => <tr className="border-t border-slate-800" key={run.id}><td className="p-3"><Link className="text-sky-300" href={`/runs/${run.id}`}>{dash(run.model)}</Link></td><td className="p-3">{run.sourceId}</td><td className="p-3">{dash(run.task)}</td><td className="p-3">{run.status}</td><td className="p-3">{dash(run.score)}</td><td className="p-3">{run.totalCostUsd === null ? "—" : `$${run.totalCostUsd.toFixed(3)}`}</td><td className="p-3">{run.durationMs === null ? "—" : `${run.durationMs}ms`}</td></tr>)}</tbody></table></div></>; }
