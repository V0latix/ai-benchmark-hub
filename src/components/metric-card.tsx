export function MetricCard({ label, value }: { label: string; value: string | number | null }) { return <section className="rounded-lg border border-slate-800 bg-slate-900/60 p-4"><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-2xl font-semibold text-white">{value ?? "—"}</p></section>; }
