import type { NormalizedRun } from "../lib/sources/types";
export function CostScoreChart({ points }: { points: NormalizedRun[] }) { const maxCost = Math.max(1, ...points.map((point) => point.totalCostUsd ?? 0)); const maxScore = Math.max(1, ...points.map((point) => point.score ?? 0)); return <svg aria-label="Cost versus score scatter plot" className="h-64 w-full rounded border border-slate-800 bg-slate-950" viewBox="0 0 600 240">{points.map((point) => <circle key={point.id} cx={40 + ((point.totalCostUsd ?? 0) / maxCost) * 530} cy={210 - ((point.score ?? 0) / maxScore) * 170} r="5" fill="#38bdf8"><title>{`${point.model ?? "Unknown"}: $${point.totalCostUsd} / ${point.score}`}</title></circle>)}</svg>; }
